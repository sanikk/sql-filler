### column insert 
dict of lists
```
 table_number = [ 
    amount,
    col1,
    col2, 
    ]
```
 access with index
 
empty fields just box.get() empty strings ''

ui should validate it's at least stringish before storing
